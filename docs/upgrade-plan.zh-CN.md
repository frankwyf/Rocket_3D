# Rocket 3D 全面升??行??（自?化版）

## 目?

本?升?聚焦三个方向：

1. **可?化能力增?**：增加可?用的?行遥?可?化数据?（折??/???色/雷???）。
2. **玩法系?增?**：修?并升?道具系?生命周期，支持可??的持???与剩余????。
3. **工程自?化增?**：CI/CD 从串行流程升??矩?流程，?出???告与可?行文件制品。

---

## 分批次?行与??

### 批次 A：可?化数据?升?

- 新增 `main/telemetry_overlay.hpp`
  - 遥??本??
  - 固定容量?史?存
  - ???分模型
  - 折?几何生成
  - ???色映射
  - 雷???生成

**??方式**
- ?元??：`[telemetry]` ????，??几何与?色?出范?、?序与?定性。

### 批次 B：核心玩法机制升?

- 更新 `main/game_mechanics.hpp`
  - ???准?依??
  - 道具效果自??期
  - 新增剩余????接口

**??方式**
- ?元??：`[mechanics]` ????，??道具激活、持?、?期全?程。

### 批次 C：CI/CD 升?

- 更新 `.github/workflows/ci.yml`
  - 增加 `workflow_dispatch`
  - 使用 `Debug/Release` 矩??建
  - ?出 JUnit ?告
  - 上????告和二?制制品

**??方式**
- Actions ?行后??：
  - ?个配置均有?建与???果
  - 存在 `test-report-*` 与 `rocket3d-bin-*` 制品

---

## 可?化??建?（手?）

?然本?新增的是“可?用可?化数据?”，仍建??行一次游?内??：

1. 在 `--profile` 模式下?行 2~3 分?。
2. 采集高度/速度/燃料序列，?? `telemetry_overlay` 生成折?点集。
3. ?????色是否随危?状?提升而偏?。
4. 在 HUD 中?加?，??折?区域不越界。

---

## 提交建?（本地）

- `feat(telemetry): add reusable overlay telemetry model and geometry builders`
- `feat(mechanics): add timed power-up lifecycle and remaining-time query`
- `ci(actions): migrate to matrix build, junit report and artifact publishing`
